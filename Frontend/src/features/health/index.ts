/**
 * Health Feature Module
 *
 * Este módulo contiene todo lo relacionado con el monitoreo de salud del backend:
 * - Componentes (BackendHealthStatus)
 * - Hooks (useBackendHealth)
 * - Servicios (healthService)
 *
 * Uso:
 *   import { BackendHealthStatus, useBackendHealth } from '@/features/health';
 */

// Components
// export { BackendHealthStatus } from './components/BackendHealthStatus';

// Hooks
// export { useBackendHealth } from './hooks/useBackendHealth';

// Services
// export { healthService } from './services/healthService';
