/**
 * Global Hooks Barrel Export
 *
 * Hooks genéricos reutilizables en toda la aplicación.
 * Los hooks específicos de cada feature van en features/[feature]/hooks/
 *
 * Uso:
 *   import { useBackendHealth } from '@/hooks';
 */

export { useBackendHealth } from './useBackendHealth';
